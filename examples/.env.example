# AWS Route53 Credentials
# Required for DNS-01 ACME challenge with Route53
#
# How to get these credentials:
# 1. Log in to AWS Console
# 2. Go to IAM > Users > Your User > Security Credentials
# 3. Create Access Key
# 4. Save the Access Key ID and Secret Access Key

AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_REGION=us-east-1

# Domain Configuration
DOMAIN=example.com

# WordPress Configuration (if using WordPress example)
DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=your_secure_password_here
DB_ROOT_PASSWORD=your_root_password_here

# Grafana Configuration (if using monitoring example)
GRAFANA_USER=admin
GRAFANA_PASSWORD=your_grafana_password_here

# Notes:
# - Never commit this file with real credentials to git
# - Copy this file to .env and fill in your real values
# - The .env file is already in .gitignore
# - Use IAM roles instead of credentials when running in AWS (ECS, EC2, etc.)
